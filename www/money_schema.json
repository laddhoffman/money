{
 "title": "Financial Setup",
 "type": "object",
 "id": "finance",
 "properties": {
  "Name": {
   "title": "Financial Setup Name",
   "type": "string",
   "description": "Name for this financial setup"
  },
  "start_date": {
   "title": "Start Date",
   "type": "string",
   "description": "Date from which to begin projection. Note that account balances should be current with this date."
  },
  "end_date": {
   "title": "End Date",
   "type": "string",
   "description": "Date to which we will carry the projection."
  },
  "default_checking": {
   "title":"Default checking account",
   "type": "string",
   "description": "Name of your default checking account"
  },
  "accounts": {
   "$ref": "schema/Accounts.json"
  },
  "income": {
   "$ref": "schema/Income.json"
  },
  "portfolio": {
   "$ref": "schema/Portfolio.json"
  },
  "loans": {
   "$ref": "schema/Loans.json"
  },
  "expenses": {
   "$ref": "schema/Expenses.json"
  }
 }
}
